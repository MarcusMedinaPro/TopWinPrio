name: 🔏 Sign Artifacts (Local Runner)

# Signing enabled for .NET 4.8+

on:
  workflow_run:
    workflows: ["🧪 Build & Test"]
    types: [completed]

permissions:
  contents: write
  actions: read

jobs:
  sign:
    if: github.event.workflow_run.conclusion == 'success'
    name: Sign artifacts on self-hosted runner
    runs-on: [self-hosted, windows, code-sign]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download unsigned build artifacts
        uses: dawidd6/action-download-artifact@v3
        with:
          name: release-bin
          path: unsigned
          workflow: build-test.yml
          run_id: ${{ github.event.workflow_run.id }}

      - name: Prepare output folder
        shell: pwsh
        run: New-Item -ItemType Directory -Force -Path signed | Out-Null

      - name: Sign artifacts with Certum SimplySign Desktop
        shell: pwsh
        env:
          CERTUM_TSA_URL: "http://time.certum.pl"
        run: |
          $signer = "C:\Program Files\Certum\SimplySign Desktop\SimplySignDesktop.exe"
          $tsa = $env:CERTUM_TSA_URL

          # Verify SimplySign Desktop exists
          if (-not (Test-Path $signer)) {
              Write-Host "❌ SimplySign Desktop not found at: $signer" -ForegroundColor Red
              Write-Host "Please install Certum SimplySign Desktop and ensure it's running" -ForegroundColor Yellow
              exit 1
          }

          $files = Get-ChildItem -Path unsigned -Recurse -Include *.exe, *.dll, *.nupkg, *.zip

          if (-not $files) {
            Write-Host "⚠️ No artifacts found to sign."
            exit 0
          }

          Write-Host "Found $($files.Count) file(s) to sign" -ForegroundColor Cyan
          $signedCount = 0
          $failedCount = 0

          foreach ($f in $files) {
            Write-Host "🔏 Signing $($f.Name)..."
            Write-Host "Command: $signer --sign `"$($f.FullName)`" --tsa `"$tsa`"" -ForegroundColor Gray

            $output = & "$signer" --sign "$($f.FullName)" --tsa "$tsa" 2>&1
            $exitCode = $LASTEXITCODE

            Write-Host "Output: $output" -ForegroundColor Gray
            Write-Host "Exit code: $exitCode" -ForegroundColor Gray

            if ($exitCode -eq 0) {
              Copy-Item $f.FullName signed/ -Force
              Write-Host "✅ Signed: $($f.Name)" -ForegroundColor Green
              $signedCount++
            } else {
              Write-Warning "❌ Failed to sign: $($f.Name) (exit code: $exitCode)"
              $failedCount++
            }
          }

          Write-Host "`nSigning Summary:" -ForegroundColor Cyan
          Write-Host "✅ Successfully signed: $signedCount" -ForegroundColor Green
          Write-Host "❌ Failed: $failedCount" -ForegroundColor Yellow

          # Continue even if some files fail - partial signing is acceptable
          if ($signedCount -eq 0) {
              Write-Error "No files were signed successfully"
              exit 1
          }

      - name: Upload signed artifacts
        uses: actions/upload-artifact@v4
        with:
          name: signed-release-bin
          path: signed/
          retention-days: 90
