name: 🧪 Build & Test

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  build-test:
    runs-on: windows-latest
    name: Build and unit test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore classic NuGet packages
        run: nuget restore TopWinPrio.sln

      - name: Restore SDK-style/PackageReference dependencies  
        run: dotnet restore TopWinPrio.Tests/TopWinPrio.Tests.csproj

      - name: Build Release configuration
        run: msbuild TopWinPrio.sln /p:Configuration=Release /m /p:Platform="Any CPU"

      - name: Run unit tests
        run: dotnet test TopWinPrio.Tests/TopWinPrio.Tests.csproj --configuration Release --no-build --logger trx

      - name: Capture build logs (Phase 2 preparation)
        run: |
          # Kör MSBuild igen med verbos logging för StyleCop analys
          msbuild TopWinPrio.sln /p:Configuration=Release /p:RunCodeAnalysis=true /flp:logfile=build-analysis.log /verbosity:normal
        continue-on-error: true

      - name: Generate SHA256 checksums
        run: |
          cd TopWinPrio.CS/bin/Release
          Get-FileHash *.exe, *.dll -Algorithm SHA256 | Select-Object Hash, @{Name="File";Expression={Split-Path $_.Path -Leaf}} | Format-Table -AutoSize | Out-File -FilePath SHA256SUMS.txt -Encoding UTF8
          Get-Content SHA256SUMS.txt

      # Phase 2: VirusTotal scanning (with enhanced debugging)
      - name: Verify VirusTotal API Key
        run: |
          Write-Host "🔍 VirusTotal API Key Check" -ForegroundColor Cyan
          if ($env:VIRUSTOTAL_API_KEY) {
            $keyLength = $env:VIRUSTOTAL_API_KEY.Length
            $keyStart = $env:VIRUSTOTAL_API_KEY.Substring(0, [Math]::Min(8, $keyLength))
            Write-Host "✅ API Key available: $keyStart... (length: $keyLength)" -ForegroundColor Green
          } else {
            Write-Host "❌ API Key not accessible" -ForegroundColor Red
          }
        env:
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}

      - name: VirusTotal Scan
        uses: crazy-max/ghaction-virustotal@v4
        id: virustotal
        continue-on-error: true
        with:
          vt_api_key: ${{ secrets.VIRUSTOTAL_API_KEY }}
          files: |
            TopWinPrio.CS/bin/Release/*.exe
            TopWinPrio.CS/bin/Release/*.dll
          vt_monitor: false
          
      - name: Save VirusTotal Results
        run: |
          Write-Host "📝 Creating VirusTotal Summary" -ForegroundColor Cyan
          
          $summary = @"
VirusTotal Scan Report - $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')
================================================================

Files Analyzed:
"@
          Get-ChildItem TopWinPrio.CS/bin/Release/*.exe, TopWinPrio.CS/bin/Release/*.dll | ForEach-Object { 
            $summary += "`n- $($_.Name) ($([math]::Round($_.Length/1KB, 2)) KB)"
          }
          
          $summary += @"

Scan Status: $($env:SCAN_STATUS)
================================================================

"@

          if ($env:SCAN_STATUS -eq "success") {
            $summary += @"
✅ VirusTotal scan completed successfully!
🔍 Check the workflow logs above for detailed scan results and URLs.
🔗 Scan results are available on VirusTotal website.

"@
          } else {
            $summary += @"
⚠️  VirusTotal scan encountered issues (this doesn't affect file safety).

Possible causes:
- API rate limiting (temporary - try again later)
- API key configuration issues  
- Network connectivity issues
- VirusTotal service temporarily unavailable

Manual Verification Steps:
1. Download the release files
2. Verify SHA256 checksums (see SHA256SUMS.txt)
3. Upload TopWinPrio.exe to https://www.virustotal.com/gui/home/upload
4. Review results from multiple antivirus engines

Expected Results: 0 detections from major engines (unsigned binaries may trigger false positives)

"@
          }

          $summary += @"
Build Security Measures:
✅ Source code publicly auditable on GitHub
✅ Build process fully automated and transparent  
✅ SonarCloud security analysis active
✅ CodeQL vulnerability scanning enabled
✅ SHA256 checksums for integrity verification
✅ Registry and P/Invoke security hardening applied

Community Verification:
Users are encouraged to perform independent scans and share results.
Report security concerns via GitHub Security Advisories.

================================================================
Generated by TopWinPrio GitHub Actions Pipeline
"@

          $summary | Out-File -FilePath "TopWinPrio.CS/bin/Release/VirusTotal-Summary.txt" -Encoding UTF8
          Write-Host "📄 Summary saved to VirusTotal-Summary.txt" -ForegroundColor Green
        env:
          SCAN_STATUS: ${{ steps.virustotal.outcome }}
        continue-on-error: true

      - name: Upload Release binaries
        uses: actions/upload-artifact@v4
        with:
          name: release-bin
          path: |
            TopWinPrio.CS/bin/Release/*.exe
            TopWinPrio.CS/bin/Release/*.dll
            TopWinPrio.CS/bin/Release/SHA256SUMS.txt
            TopWinPrio.CS/bin/Release/VirusTotal-Summary.txt

      - name: Upload build analysis logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-logs
          path: |
            build-analysis.log

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            **/TestResults/**/*.trx
